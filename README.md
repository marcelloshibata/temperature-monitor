# Monitor de Temperatura em C++/CLI
Um monitor de temperatura de CPU e GPU que utiliza a linguagem C++ e sua extensão C++/CLI da Microsoft que integra o ambiente .NET. Possui dependência da DLL LibreHardwareMonitor e é necessário um driver interno (como PawnIO) para ler a temperatura da CPU.
